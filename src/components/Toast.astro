<div
  class="bg-primary fixed right-0 bottom-0 left-0 m-4 hidden items-center gap-1 rounded-sm border py-1 pr-4 pl-2 opacity-0 sm:right-auto md:text-lg"
  id="toast-container"
>
  <span> Toast </span>
</div>
<script is:inline>
  window.localStorage.removeItem('form-busy')
  function toast(message, type) {
    const toastContainer = document.getElementById('toast-container')
    if (toastContainer) {
      toastContainer.classList.remove('hidden')
      toastContainer.classList.add('inline-flex')
      toastContainer.animate(
        [
          { opacity: '0', transform: 'translateY(100%)' },
          { opacity: '1', transform: 'translateY(0%)' }
        ],
        {
          duration: 150,
          easing: 'ease-in-out',
          fill: 'forwards'
        }
      )

      const success =
        '<div style="width: 10px; height: 10px; border-radius: 2px; background-color: green;"></div>'
      const error =
        '<div style="width: 10px; height: 10px; border-radius: 2px; background-color: red;"></div>'

      toastContainer.innerHTML = `<p style="font-size: 12px;">${type === 'success' ? success : error}</p> <p>${message}</p>`

      toastContainer.animate(
        [
          { opacity: '1', transform: 'translateY(0%)' },
          { opacity: '0', transform: 'translateY(100%)' }
        ],
        {
          delay: 3000,
          duration: 150,
          easing: 'ease-in-out',
          fill: 'forwards'
        }
      ).onfinish = () => {
        toastContainer.classList.add('hidden')
        window.localStorage.removeItem('form-busy')
      }
    }
  }
</script>
